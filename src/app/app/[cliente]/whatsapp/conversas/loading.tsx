import { EsqueletoConversas } from '@/components/esqueleto';

// Esqueleto dedicado do CRM de 3 colunas — a tela mais pesada e distinta.
export default function Loading() {
  return <EsqueletoConversas />;
}
